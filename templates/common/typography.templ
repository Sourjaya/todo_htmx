package common

templ Typography(variant string, label string, classNames string) {
	if variant == "header" {
		<h1 class={ "text-white font-exo2 text-5xl", classNames }>{ label }</h1>
	} else {
		<p class={ "text-black font-exo2 text-sm", classNames }>{ label }</p>
	}
}
